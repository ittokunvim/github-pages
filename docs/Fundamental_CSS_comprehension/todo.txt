setup
- [x] ファイルを作成する。
- [x] linkでCSSを読み込む。
- [x] CSS resource fileをCSSにコピペする。
- [x] CSSを読み込める形にする。

CSS selector and roleset
- [x] 4つのセレクタの詳細度をコメントに書く。
- [x] 4つのセレクタに正しいルールセットに配置する。
- [x] CSSのエラーを解決する。

add roleset
- [x] ヘッダとフッタは、合計の高さ50px, paddingは全側面合計10pxで指定。
- [x] h2, p要素のマージンを0に設定。
- [x] article要素の高さを120pxに指定（emで指定すること）。これにより画像がはみ出ない。背景色を半透明の黒にする。
- [x] h2要素にfont-size: 20px;を指定（emで指定すること）。コンテンツの高さの合計が30pxにする。
- [x] footer p要素にfont-size: 15pxを指定（em）。コンテンツの高さの合計が30pxにする。
- [x] articleのパディングを調整、フォントの色を明るい色にする。

etc...
- [x] CSSを読みやすくする。
- [x] 全てのセレクタの先頭に.cardを含める。

